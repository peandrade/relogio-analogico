{
  "name": "relogio-analogico",
  "version": "1.0.0",
  "type": "module",
  "description": "Um relógio analógico funcional desenvolvido com HTML, CSS e JavaScript. Ele mostra a hora atual tanto em formato analógico (com ponteiros) quanto digital, atualizando automaticamente a cada segundo.",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prettier:check": "prettier --check .",
    "prettier:fix": "prettier --write .",
    "stylelint": "stylelint \"src/**/*.css\"",
    "stylelint:fix": "stylelint \"src/**/*.css\" --fix",
    "build": "echo 'Nenhum build necessário para HTML/CSS/JS puro' && exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^9.23.0",
    "prettier": "^3.5.3",
    "stylelint": "^16.16.0",
    "stylelint-config-standard": "^37.0.0"
  }
}
